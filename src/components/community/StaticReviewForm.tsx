/** Vitrine statique (GitHub Pages) : pas de serveur, donc pas de dépôt d'avis. Même signature que ReviewForm. */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function ReviewForm(_props: { recipeId: string }) {
  return <p className="card p-5 text-sm text-muted">Le dépôt d&apos;avis est disponible sur la version en ligne du site.</p>;
}
